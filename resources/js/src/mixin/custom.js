

//AFTER MULTI ADD REMOVE

/*
appendJsonData(data,array){
  this[array].push(JSON.parse(data))
  this[array].splice(0,1)
  this[array] = this[array][0]
  return this[array]
}

removeThis(index) {
  this.description.splice(index,1);
},
addOptions() {
  this.description.push({
    value: ''
  });
},*/


// created () {
//     this.$store.dispatch('custom/fetchItems')
//       .then((response) => {
//         response.data.forEach(element => {
//           this[element.type].push(JSON.parse(element.JsonData)[element.type])
//           this[element.type].splice(0, 1)
//           this[element.type] = this[element.type][0]
//         })

//       })
//       .catch((error) => { console.log(error) })

//   }



// addOptions (type) {
//     this[type].push({
//     })
//   },
